<% if game.empty? %>
<div class="table-row text-sm font-normal text-left">
<% else %>
<div class="table-row text-sm font-normal text-left hover:bg-gray-50 cursor-pointer" data-controller="games" data-action="click->games#show" data-games-id="<%= game.id %>" id="<%= dom_id game %>">
<% end %>
  <div class="table-cell pl-4 py-3.5 w-1/12 max-sm:hidden text-center align-middle">
    <span><%= game.emoji %></span>
  </div>
  <div class="table-cell px-4 py-3.5 w-6/12">
    <h2 class="font-medium text-gray-800"><%= game.display_name %></h2>
    <p class="text-gray-600"><%= game.designer.name %></p>
  </div>
  <div class="table-cell px-4 py-3.5 w-3/12 max-sm:hidden align-middle">
    <div class="<%= play_time_badge_classes(game) %>">
      <%= game.game_length_in_minutes %>
    </div>
  </div>
  <div class="table-cell px-4 py-3.5 w-2/12 max-sm:hidden align-middle">
    <h4 class="text-gray-700"><%= game.number_of_players %></h4>
  </div>
</div>
